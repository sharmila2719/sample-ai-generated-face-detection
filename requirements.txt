# Runtime dependencies (also see infrastructure/lambda_requirements.txt for Lambda packaging)
boto3==1.35.0
botocore==1.35.0
pillow==10.4.0

# Development / test dependencies
pytest==8.3.2
pytest-asyncio==0.23.8
moto[s3,dynamodb,sqs,sns,sagemaker]==5.0.10
hypothesis==6.112.0
aws-cdk-lib==2.150.0
constructs==10.3.0
